export interface IHandle {
    (...args: any[]): void;
}
export default class TinyEmitter {
    private e;
    constructor();
    on(name: string, handle: IHandle): void;
    off(name: string, handle?: IHandle): void;
    emit(name: string, ...args: any[]): void;
}
